/**
 * StadiumOPS — operations alias routing layout
 * Exposes the operations command center layout via /operations route
 */

import CommandLayout from '../command/layout';

export default CommandLayout;
